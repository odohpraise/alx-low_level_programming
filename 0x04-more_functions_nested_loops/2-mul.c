#include "holberton.h"
#include <stdio.h>
/**
 * mul - multiplies two integer
 * @a: integer to be multiplied
 * @b: value to be multiplied
 * Return: 0 always success
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
